{
    "data_dir": "./data",
    "labels": "labels.txt",
    "model_name_or_path": "bert-base-uncased",
    "output_dir": "./output",
    "logging_dir": "./logging",
    "max_seq_length": 512,
    "num_train_epochs": 5,
    "per_device_train_batch_size": 8,
    "per_device_eval_batch_size": 8,
    "save_steps": 10000,
    "seed": 1,
    "n_gpu": 3,
    "do_train": true,
    "do_eval": true,
    "do_predict": true
}